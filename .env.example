# Environment variables for Docker Compose
# Copy this file to .env and update with your own secure passwords
# IMPORTANT: Change all default passwords before deploying to production!

# SQL Server Configuration
# SECURITY: Replace with a strong password (min 8 chars, uppercase, lowercase, digits, special chars)
SA_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
MSSQL_PID=Developer

# Database Configuration
DB_NAME=AccountMovs
DB_SERVER=sqlserver

# API Configuration
ASPNETCORE_ENVIRONMENT=Production
